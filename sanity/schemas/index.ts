// Sanity schema registry. Phase 1 fills this with content models:
//   site, page, navItem, cta, service, instructor, faq, testimonial, seo
// For Phase 0 (light scaffold), the array is empty so Studio loads without errors.

import type { SchemaTypeDefinition } from "sanity";

export const schemaTypes: SchemaTypeDefinition[] = [];
